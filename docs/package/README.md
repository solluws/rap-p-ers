# Package internals

Notes about how parts of the library are put together. Moved out of the
`rootpy/` package so the installed wheel is code only.

- [`rootpy-internals.md`](rootpy-internals.md) — module layout, the layering,
  lazy registries, response shapes, the gateway, multi-account hosting, and
  where the time goes
- [`direct-messages.md`](direct-messages.md) — how the DM services fit together

For using the SDK, start with [`../../README.md`](../../README.md) or
[`../api.md`](../api.md).
